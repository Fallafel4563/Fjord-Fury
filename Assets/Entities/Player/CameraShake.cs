using Unity.Cinemachine;
using UnityEngine;
using UnityEngine.Playables;

/*public class CameraShake : MonoBehaviour
{
    //public GameObject shakeGroup;
    //public GameObject shakePosition;
    //public float shakeViolence;
    //public Animator shakeAnimator;
    //public GameObject cameraObject;
    public bool cameraShakeToggle;
    //private Vector3 violenceScale;
    //public PlayableDirector PlayableDirector;
    public CinemachineCollisionImpulseSource impulseSource;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        cameraShakeToggle = false;
       // PlayableDirector = GetComponent<PlayableDirector>();
        //shakeAnimator = GetComponent<Animator>();
        //GameObject.Find("ShakePosition").GetComponent<Animator>();
        impulseSource = GetComponent<CinemachineCollisionImpulseSource>();
    }

    // Update is called once per frame
    void Update()
    {
        
       
    }

    public void CameraShakeEnable()
    {
        if (cameraShakeToggle == true)
        {
            //shakeGroup.transform.localScale = new Vector3(shakeViolence, shakeViolence, shakeViolence);
            //CameraAnimationStart();
            impulseSource.GenerateImpulse();
            
            Debug.Log("CameraShake Enabled");
            
        }
    }

    public void CameraAnimationStart()
    {
        //shakeAnimator.Play("CameraShake Animation"); 
        //Debug.Log("Camera-Shake Animation Played");
        //PlayableDirector.Play();
        //cameraObject.transform.position = shakePosition.transform.position;
    }
}*/
